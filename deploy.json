{
    "apps": [
        {
            "name": "mathist",
            "script": "./node_modules/next/dist/bin/next",
            "instances": 15,
            "args": "dev",
            "env": {
                "PORT": "3000"
            }
        }
    ]
}